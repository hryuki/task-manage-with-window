# Task Manager with Window

タスクごとにウィンドウやChromeタブを紐づけて管理するmacOSアプリ

## 機能

- 📋 階層的なタスク管理
- 🖥 タスクにアプリウィンドウを紐づけ
- 🌐 タスクにChromeタブを紐づけ
- 🔄 タスク選択時に関連ウィンドウ/タブを自動でアクティブ化
- 📍 メニューバー常駐 + フローティングウィンドウ

## システム要件

- macOS 10.15以上
- Node.js 18以上
- Chrome（タブ連携を使用する場合）

## セットアップ

### 1. Electronアプリ

```bash
cd electron-app

# 依存関係をインストール
npm install

# 開発モードで起動
npm run dev

# 別のターミナルで
npm start
```

### 2. Chrome拡張機能

```bash
cd chrome-extension

# 依存関係をインストール
npm install

# ビルド
npm run build
```

ビルド後、Chromeで以下の手順で拡張機能をインストール：

1. Chrome で `chrome://extensions/` を開く
2. 「デベロッパーモード」をONにする
3. 「パッケージ化されていない拡張機能を読み込む」をクリック
4. `chrome-extension` フォルダを選択

## 使い方

1. アプリを起動するとメニューバーにアイコンが表示されます
2. アイコンをクリックするとフローティングウィンドウが表示されます
3. 「新しいタスクを追加...」からタスクを作成
4. タスクの「+ウィンドウ」ボタンでウィンドウ/タブを紐づけ
5. 「切替」ボタンで紐づいたウィンドウ/タブがアクティブになります

## アクセシビリティ権限

ウィンドウ制御のため、以下の権限が必要です：

1. システム環境設定 > セキュリティとプライバシー > プライバシー > アクセシビリティ
2. Task Managerアプリを追加して許可

## プロジェクト構成

```
.
├── electron-app/           # Electronアプリ
│   ├── src/
│   │   ├── main/          # メインプロセス
│   │   ├── renderer/      # レンダラー（React UI）
│   │   └── shared/        # 共通型定義
│   └── package.json
│
└── chrome-extension/       # Chrome拡張機能
    ├── src/
    │   ├── background.ts  # WebSocket通信
    │   └── types.ts       # 型定義
    ├── manifest.json
    └── package.json
```

## 技術スタック

- **Electron** - デスクトップアプリフレームワーク
- **React** - UIフレームワーク
- **TypeScript** - 型安全な開発
- **SQLite (better-sqlite3)** - ローカルデータ保存
- **WebSocket** - Chrome拡張との通信
- **osascript (AppleScript)** - macOSウィンドウ制御
